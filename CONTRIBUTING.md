# Руководство для контрибьюторов

Спасибо за интерес к проекту **Glyph**! Мы строим систему, которая должна быть безопасной, надёжной и долговечной. Поэтому к коду предъявляются высокие требования.

## 🧭 Процесс внесения изменений

1. **Обсуждение**: Перед началом работы над крупной фичей создайте Issue с описанием предложения.
2. **Форк и ветка**: Работайте в своей ветке, названной осмысленно (`feature/awesome-addon`, `fix/issue-123`).
3. **Код и стиль**:
   * Python: следуйте PEP 8, используйте type hints, docstring в стиле Google.
   * Rust: следуйте официальному стилю (`rustfmt`), документируйте публичные функции.
   * Комментарии и документация обязательны для новых модулей.
4. **Тестирование**:
   * Добавляйте тесты на новый функционал (модульные в `tests/`).
   * Убедитесь, что все тесты проходят: `make test`.
   * Для изменений, влияющих на производительность, приложите результаты нагрузочных тестов.
5. **Запуск линтеров**: Перед коммитом выполните `make lint` (если такой появится) или проверьте код вручную.
6. **Commit message**: Пишите понятные сообщения на английском или русском. Хороший пример: `feat(crypto): add Argon2id key derivation`.
7. **Pull Request (PR)**:
   * Опишите, что сделано и почему.
   * Ссылайтесь на связанные Issue.
   * Убедитесь, что CI (GitHub Actions) проходит успешно.

## ✅ Что проверяется в PR

* Корректность архитектуры и соответствие модульности.
* Наличие и прохождение тестов.
* Отсутствие явных уязвимостей (будут добавлены автоматические проверки позже).
* Документирование нового функционала в `docs/`.

## 🤔 Вопросы?

Если что-то непонятно, создайте Issue с меткой `question`. Мы постараемся ответить быстро.
